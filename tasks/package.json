{
  "name": "tasks",
  "version": "1.0.0",
  "description": "",
  "main": "migrate-file-name.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "babel-node migrate-file-name.js",
    "build": "browserify migrate-file-name.js -o migrate-file-name.min.js --full-paths=false --node"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserify": "^13.0.1",
    "fs-extra": "^0.30.0",
    "walker": "^1.0.7"
  },
  "devDependencies": {
  }
}
